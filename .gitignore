# Dependencies
node_modules/

# Environment — contains secrets, never commit
.env
.env.local

# Builds
dist/
.dist/

# Uploaded media (backed up separately, not in git)
server/uploads/

# Logs / OS noise
*.log
.DS_Store
Thumbs.db
